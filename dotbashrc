if [[ ! -d /home/share/workspace ]] ; then
sudo mkdir /home/share/workspace
sudo chown lmpuser: /home/share/workspace
cp -r /home/lmpuser/lmpui /home/share/workspace
fi

if [ -f "$HOME/venv/bin/activate" ]; then
    . "$HOME/venv/bin/activate"
fi

source ~/venv/bin/activate
